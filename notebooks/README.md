# Notebooks

Reproducible analyses live as scripts for now:

- [`docs/make_figures.py`](../docs/make_figures.py) regenerates every README
  figure from the dataset + reference kernel.

Executable notebooks run under `nbmake` in CI (the family convention) arrive with
Phase B, alongside the dynamic-hERG model and the validation compound set.
